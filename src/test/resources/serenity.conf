serenity {
  take.screenshots = AFTER_EACH_STEP
  console.headings = normal
  reports.show.step.details = true
  logging = VERBOSE
  test.root = "starter.tests"
}

headless.mode = false
webdriver {
  driver = remote
  remote {
    url="http://localhost:8085"
    driver=chrome
  }
}

